 new Swiper('.brands-list-wrapper', {        pagination: {            el: '.swiper-pagination',            clickable: true,        },        watchOverflow: true,        spaceBetween: 16,        slidesPerView: 'auto',    });    const buttonMoreText = document.querySelector('.read-more-btn');    const brandsList = document.querySelector('.brands-list');    const brands = brandsList.children;    const hiddenForTablet = document.querySelectorAll('.brands-list__item');    let clickHiddenClick = function () {        const span = buttonMoreText.querySelector('.read-more-btn__text');        const icon = buttonMoreText.querySelector('.read-more-btn__icon');        for (const brand of brands) {            if (brand.classList.contains('hidden') || brand.classList.contains('tablet-hidden')) {                brand.classList.remove('hidden')            } else if (brand.classList.contains('for-hidden')) {                brand.classList.add('hidden');            }        }        if (span.textContent === 'Показать все') {            span.textContent = 'Скрыть';            icon.classList.add('read-more-btn__icon--rotate');        } else {            span.textContent = 'Показать все';            icon.classList.remove('read-more-btn__icon--rotate');        }    };    buttonMoreText.addEventListener('click', clickHiddenClick);